services:
  frontend:
    build: ./frontend
    ports:
      - "8080:80"

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      TRANSIT_511_API_KEY: "${TRANSIT_511_API_KEY}"
